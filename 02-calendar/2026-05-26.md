---
created: 2026-05-26
day: Tuesday
tags: [daily]
mood: 
focus: k-safety-law-rag
---

# 2026-05-26 (Tue)

## 오늘 할 일
- [x] `run_ingest.py`에 `--dir` 옵션 추가
- [x] thinking 모드 문제 심층 디버깅
- [x] CPU/GPU 버전 분리, 스트리밍 방식 전환

## 공부/개발 기록
- `--dir` 옵션으로 특정 하위 폴더만 재임베딩 가능하도록 개선
- `rag/chatbot.py`(GPU, num_ctx=8192) / `rag/chatbot_cpu.py`(CPU, num_gpu=0) 버전 분리
- `stream=True`로 전환해 토큰 생성 즉시 출력, `<think>` 블록 감지 시 "[추론 중....]" 표시
- thinking 모드 디버깅 3차 시도 끝에 근본 원인 파악: RTX 3050 VRAM 초과로 6.9GB 모델이 CPU/GPU 분할 실행 중이었음
- 근본 해결: qwen3.5:4b(3.4GB, thinking ON) → qwen2.5:3b(2GB, non-thinking, 한국어 우수)로 모델 교체

## 에러 & 해결
- thinking 모드 문제의 근본 원인(VRAM 초과 + Ollama 0.24.0의 think 파라미터 미지원)을 확인하고 모델 교체로 최종 해결. [[error-2026-05-25-ollama-thinking-mode]]

## 취준 활동
- 해당 없음

## 내일 할 일
- [ ] qwen2.5:3b 실제 응답 속도 및 답변 품질 확인
- [ ] 스트리밍 동작 여부 재확인
