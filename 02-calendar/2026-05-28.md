---
created: 2026-05-28
day: Thursday
tags: [daily]
mood: 
focus: k-safety-law-rag
---

# 2026-05-28 (Thu)

## 오늘 할 일
- [x] 텍스트 법령 RAG와 표 추출 `rag_table` 코드를 하나의 `rag` 패키지로 통합
- [x] 기존 버그 수정 (config/embedder/vector_store/ingest)
- [x] 전체 스모크 테스트 작성

## 공부/개발 기록
- `CLAUDE_v1.1.md` 재작성 — 실제 폴더 구조(`data/laws`, `chroma_db`가 루트 기준) 반영
- `rag/config.py` 경로 정리, `rag/embedder.py` import 경로 수정, `rag/vector_store.py` ChromaDB telemetry 오류 억제
- `test_run.py` 스모크 테스트 진입점 작성 — 기본 테스트(모델 로딩 없이 라이브러리/경로/데이터 확인) + `--full` 옵션(bge-m3 로딩, 1024차원 벡터, ChromaDB upsert/query/delete)
  - 결과: `passed=13, failed=0` (기본), `passed=15, failed=0` (`--full`)
- `rag_table`의 표 추출/청킹/검색/보고서 코드를 `rag.table_extraction`, `rag.table_chunking`, `rag.table_vector_store`, `rag.table_retriever`, `rag.table_report`, `rag.table_pipeline`으로 통합
- 법령 PDF 표 추출량 확인: 전체 296개 표, 949개 row (산업안전보건법_시행규칙 259개로 대부분)
- 텍스트 법령 RAG(70개 청크), 표 법령 RAG(11개 청크) 임베딩 상태 확인

## 에러 & 해결
- 특이 버그 없음. 통합/정리 작업 위주.

## 취준 활동
- 해당 없음

## 내일 할 일
- [ ] `claude_code_prompt_v2.md` 기준 표 RAG 파이프라인 버그 수정
