---
created: 2026-06-15
day: Monday
tags: [daily]
mood: 
focus: k-safety-law-rag
---

# 2026-06-15 (Mon)

## 오늘 할 일
- [x] 프론트 구조를 ChatGPT형 레이아웃으로 전체 재작성
- [x] QuestionScopeNode / LangGraph 기반 라우팅 도입
- [x] 계정별 DB 관리 구조 확정

## 공부/개발 기록
- 좌측 사이드바, 모드별 대화 목록, 중앙 채팅 스트림, 하단 고정 입력창 구조로 전체 프론트 재작성 (기존 파스텔 블루/네이비 색상은 유지)
- 진입 화면 추가: `localhost:8200` 접속 시 챗봇 소개 화면 → "다음" 버튼으로 로그인 이동
- 새 상담 화면에 시나리오 모드/일반 모드 설명과 예시 질문 버튼 제공, 채팅 목록을 두 섹션으로 분리
- 관리자 계정(CLI 전체 출력, 모델명, 응답시간, 상세 근거) vs 일반 계정(답변만) 권한 분리, 기본 포트 8200으로 통일
- `QuestionScopeNode` 추가: 단순 법령 질문(`general_law`) vs 시나리오 판단 질문(`scenario_judgment`) 분류, 일반 모드에서는 "위 사고" 표현이 있어도 강제로 시나리오 판단형으로 이동하지 않도록 처리
- `rag/question_graph.py` 신규 — 외부 langgraph 의존성 없는 경량 그래프 라우팅 모듈, 추후 IntentClassifierNode/CitationValidatorNode/CacheGuardNode 확장 가능한 구조로 설계
- 비계 특별안전교육 질문을 단순 질문(조건부 법령 설명) vs 시나리오 질문(위반 여부 판단형)으로 분기
- EXAONE이 `PRIMARY TEXT`, `BACKGROUND [table]`, `score=...` 같은 내부 RAG 라벨을 답변에 그대로 복사하지 않도록 프롬프트/후처리 보강
- `users`, `sessions`, `scenarios`, `conversations`, `messages`, `deletion_logs` 테이블을 `user_id`/`conversation_id` 기준으로 분리, 관리자/일반 계정은 role과 user_id로 같은 DB 안에서 분리 관리

## 에러 & 해결
- 특이 버그 없음. 아키텍처 확장(라우팅 그래프, 계정 분리) 위주.

## 취준 활동
- 해당 없음

## 내일 할 일
- [ ] IntentClassifierNode / CitationValidatorNode / CacheGuardNode 구현
- [ ] 보고서 PDF 렌더링 파이프라인 연결
